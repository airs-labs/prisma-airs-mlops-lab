"""Legacy inference module.

The primary inference interface is now inference_client.py which supports
Vertex AI and local backends for causal language model inference.

This module is retained for backwards compatibility.
"""

# Inference is now handled by inference_client.py
# See: airs_mlops_lab.serving.inference_client
